import { useState } from 'react';
import styles from '../../styles/Blogger.module.css';

const UserPosts = ({ post, openModal }) => {
	const onClickHandler = (e) => {
		setEditMode(true);
	};
	return <></>;
};

export default UserPosts;
